/*
 * Copyright 2019-present Infosys Limited
 *
 * SPDX-License-Identifier: Apache-2.0
 */
/******************************************************************************
 *
 * This is an auto generated file.
 * Please do not edit this file.
 * All edits to be made through template source file
 * <TOP-DIR/scripts/GtpV2StackCodeGen/tts/ietemplate.cpp.tt>
 ******************************************************************************/

#include "cmiIe.h"
#include "dataTypeCodecUtils.h"

CmiIe::CmiIe() 
{
    ieType = 148;
    // TODO

}

CmiIe::~CmiIe() {
    // TODO Auto-generated destructor stub
}

bool CmiIe::encodeCmiIe(MsgBuffer &buffer, CmiIeData const &data)
{
    buffer.skipBits(7);

    if(!(buffer.writeBits(data.cmi, 1)))
    {
        errorStream.add((char *)"Encoding of cmi failed\n");
        return false;
    }

    return true;
}

bool CmiIe::decodeCmiIe(MsgBuffer &buffer, CmiIeData &data, Uint16 length)
{     
    // TODO optimize the length checks
    
    Uint16 ieBoundary = buffer.getCurrentIndex() + length;
    buffer.skipBits(7);
    if (buffer.getCurrentIndex() > ieBoundary)
    {
        errorStream.add((char *)"Attempt to read beyond IE boundary: \n");
        return false;
    }

    data.cmi = buffer.readBits(1);
    // confirm that we are not reading beyond the IE boundary
    if (buffer.getCurrentIndex() > ieBoundary)
    {
        errorStream.add((char *)"Attempt to read beyond IE boundary: cmi\n");
        return false;
    }

    // The IE is decoded now. The buffer index should be pointing to the 
    // IE Boundary. If not, we have some more data left for the IE which we don't know
    // how to decode
    if (ieBoundary == buffer.getCurrentIndex())
    {
        return true;
    }
    else
    {
        errorStream.add((char *)"Unable to decode IE CmiIe\n");
        return false;
    }
}
void CmiIe::displayCmiIe_v(CmiIeData const &data, Debug &stream)
{
    stream.incrIndent();
    stream.add((char *)"CmiIeData:");
    stream.incrIndent();
    stream.endOfLine();
  
    stream.add( (char *)"cmi: "); 
    stream.add((Uint8)data.cmi);
    stream.endOfLine();
    stream.decrIndent();
    stream.decrIndent();
}
