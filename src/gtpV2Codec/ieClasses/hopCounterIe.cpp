/*
 * Copyright 2019-present Infosys Limited
 *
 * SPDX-License-Identifier: Apache-2.0
 */
/******************************************************************************
 *
 * This is an auto generated file.
 * Please do not edit this file.
 * All edits to be made through template source file
 * <TOP-DIR/scripts/GtpV2StackCodeGen/tts/ietemplate.cpp.tt>
 ******************************************************************************/

#include "hopCounterIe.h"
#include "dataTypeCodecUtils.h"

HopCounterIe::HopCounterIe() 
{
    ieType = 113;
    // TODO

}

HopCounterIe::~HopCounterIe() {
    // TODO Auto-generated destructor stub
}

bool HopCounterIe::encodeHopCounterIe(MsgBuffer &buffer, HopCounterIeData const &data)
{
    if (!(buffer.writeUint8(data.hopCounter)))
    {
        errorStream.add((char *)"Encoding of hopCounter failed\n");
        return false;
    }

    return true;
}

bool HopCounterIe::decodeHopCounterIe(MsgBuffer &buffer, HopCounterIeData &data, Uint16 length)
{     
    // TODO optimize the length checks
    
    Uint16 ieBoundary = buffer.getCurrentIndex() + length;

    buffer.readUint8(data.hopCounter);
    if (buffer.getCurrentIndex() > ieBoundary)
    {
        errorStream.add((char *)"Attempt to read beyond IE boundary: hopCounter\n");
        return false;
    }

    // The IE is decoded now. The buffer index should be pointing to the 
    // IE Boundary. If not, we have some more data left for the IE which we don't know
    // how to decode
    if (ieBoundary == buffer.getCurrentIndex())
    {
        return true;
    }
    else
    {
        errorStream.add((char *)"Unable to decode IE HopCounterIe\n");
        return false;
    }
}
void HopCounterIe::displayHopCounterIe_v(HopCounterIeData const &data, Debug &stream)
{
    stream.incrIndent();
    stream.add((char *)"HopCounterIeData:");
    stream.incrIndent();
    stream.endOfLine();
  
    stream.add((char *)"hopCounter: ");
    stream.add(data.hopCounter);
    stream.endOfLine();
    stream.decrIndent();
    stream.decrIndent();
}
